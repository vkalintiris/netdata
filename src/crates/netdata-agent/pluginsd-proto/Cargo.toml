[package]
name = "netdata-agent-pluginsd-proto"
description = "The plugins.d / streaming text protocol codec of the netdata agent"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
netdata-agent-text = { workspace = true }

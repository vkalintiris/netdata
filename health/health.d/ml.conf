# apps_bandwidth_tcp_recv
template: ml_1min_apps_bandwidth_tcp_recv
      on: apps.bandwidth_tcp_recv_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_bandwidth_tcp_recv score for the last 1 minutes
        

# apps_bandwidth_tcp_retransmit
template: ml_1min_apps_bandwidth_tcp_retransmit
      on: apps.bandwidth_tcp_retransmit_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_bandwidth_tcp_retransmit score for the last 1 minutes
        

# apps_bandwidth_tcp_send
template: ml_1min_apps_bandwidth_tcp_send
      on: apps.bandwidth_tcp_send_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_bandwidth_tcp_send score for the last 1 minutes
        

# apps_bandwidth_udp_recv
template: ml_1min_apps_bandwidth_udp_recv
      on: apps.bandwidth_udp_recv_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_bandwidth_udp_recv score for the last 1 minutes
        

# apps_bandwidth_udp_send
template: ml_1min_apps_bandwidth_udp_send
      on: apps.bandwidth_udp_send_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_bandwidth_udp_send score for the last 1 minutes
        

# apps_cpu
template: ml_1min_apps_cpu
      on: apps.cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_cpu score for the last 1 minutes
        

# apps_cpu_system
template: ml_1min_apps_cpu_system
      on: apps.cpu_system_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_cpu_system score for the last 1 minutes
        

# apps_cpu_user
template: ml_1min_apps_cpu_user
      on: apps.cpu_user_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_cpu_user score for the last 1 minutes
        

# apps_file_closed
template: ml_1min_apps_file_closed
      on: apps.file_closed_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_file_closed score for the last 1 minutes
        

# apps_file_deleted
template: ml_1min_apps_file_deleted
      on: apps.file_deleted_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_file_deleted score for the last 1 minutes
        

# apps_file_open
template: ml_1min_apps_file_open
      on: apps.file_open_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_file_open score for the last 1 minutes
        

# apps_files
template: ml_1min_apps_files
      on: apps.files_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_files score for the last 1 minutes
        

# apps_lreads
template: ml_1min_apps_lreads
      on: apps.lreads_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_lreads score for the last 1 minutes
        

# apps_lwrites
template: ml_1min_apps_lwrites
      on: apps.lwrites_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_lwrites score for the last 1 minutes
        

# apps_major_faults
template: ml_1min_apps_major_faults
      on: apps.major_faults_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_major_faults score for the last 1 minutes
        

# apps_mem
template: ml_1min_apps_mem
      on: apps.mem_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_mem score for the last 1 minutes
        

# apps_minor_faults
template: ml_1min_apps_minor_faults
      on: apps.minor_faults_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_minor_faults score for the last 1 minutes
        

# apps_pipes
template: ml_1min_apps_pipes
      on: apps.pipes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_pipes score for the last 1 minutes
        

# apps_preads
template: ml_1min_apps_preads
      on: apps.preads_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_preads score for the last 1 minutes
        

# apps_process_create
template: ml_1min_apps_process_create
      on: apps.process_create_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_process_create score for the last 1 minutes
        

# apps_processes
template: ml_1min_apps_processes
      on: apps.processes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_processes score for the last 1 minutes
        

# apps_pwrites
template: ml_1min_apps_pwrites
      on: apps.pwrites_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_pwrites score for the last 1 minutes
        

# apps_sockets
template: ml_1min_apps_sockets
      on: apps.sockets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_sockets score for the last 1 minutes
        

# apps_swap
template: ml_1min_apps_swap
      on: apps.swap_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_swap score for the last 1 minutes
        

# apps_task_close
template: ml_1min_apps_task_close
      on: apps.task_close_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_task_close score for the last 1 minutes
        

# apps_thread_create
template: ml_1min_apps_thread_create
      on: apps.thread_create_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_thread_create score for the last 1 minutes
        

# apps_threads
template: ml_1min_apps_threads
      on: apps.threads_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_threads score for the last 1 minutes
        

# apps_total_bandwidth_recv
template: ml_1min_apps_total_bandwidth_recv
      on: apps.total_bandwidth_recv_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_total_bandwidth_recv score for the last 1 minutes
        

# apps_total_bandwidth_sent
template: ml_1min_apps_total_bandwidth_sent
      on: apps.total_bandwidth_sent_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_total_bandwidth_sent score for the last 1 minutes
        

# apps_uptime_avg
template: ml_1min_apps_uptime_avg
      on: apps.uptime_avg_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_uptime_avg score for the last 1 minutes
        

# apps_uptime
template: ml_1min_apps_uptime
      on: apps.uptime_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_uptime score for the last 1 minutes
        

# apps_uptime_max
template: ml_1min_apps_uptime_max
      on: apps.uptime_max_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_uptime_max score for the last 1 minutes
        

# apps_uptime_min
template: ml_1min_apps_uptime_min
      on: apps.uptime_min_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_uptime_min score for the last 1 minutes
        

# apps_vfs_read_bytes
template: ml_1min_apps_vfs_read_bytes
      on: apps.vfs_read_bytes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_vfs_read_bytes score for the last 1 minutes
        

# apps_vfs_read_call
template: ml_1min_apps_vfs_read_call
      on: apps.vfs_read_call_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_vfs_read_call score for the last 1 minutes
        

# apps_vfs_write_bytes
template: ml_1min_apps_vfs_write_bytes
      on: apps.vfs_write_bytes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_vfs_write_bytes score for the last 1 minutes
        

# apps_vfs_write_call
template: ml_1min_apps_vfs_write_call
      on: apps.vfs_write_call_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_vfs_write_call score for the last 1 minutes
        

# apps_vmem
template: ml_1min_apps_vmem
      on: apps.vmem_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average apps_vmem score for the last 1 minutes
        

# cpu_cpu0_cpuidle
template: ml_1min_cpu_cpu0_cpuidle
      on: cpu.cpu0_cpuidle_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average cpu_cpu0_cpuidle score for the last 1 minutes
        

# cpu_cpu0_interrupts
template: ml_1min_cpu_cpu0_interrupts
      on: cpu.cpu0_interrupts_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average cpu_cpu0_interrupts score for the last 1 minutes
        

# cpu_cpu0
template: ml_1min_cpu_cpu0
      on: cpu.cpu0_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average cpu_cpu0 score for the last 1 minutes
        

# cpu_cpu0_softirqs
template: ml_1min_cpu_cpu0_softirqs
      on: cpu.cpu0_softirqs_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average cpu_cpu0_softirqs score for the last 1 minutes
        

# cpu_cpu0_softnet_stat
template: ml_1min_cpu_cpu0_softnet_stat
      on: cpu.cpu0_softnet_stat_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average cpu_cpu0_softnet_stat score for the last 1 minutes
        

# cpu_cpu1_cpuidle
template: ml_1min_cpu_cpu1_cpuidle
      on: cpu.cpu1_cpuidle_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average cpu_cpu1_cpuidle score for the last 1 minutes
        

# cpu_cpu1_interrupts
template: ml_1min_cpu_cpu1_interrupts
      on: cpu.cpu1_interrupts_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average cpu_cpu1_interrupts score for the last 1 minutes
        

# cpu_cpu1
template: ml_1min_cpu_cpu1
      on: cpu.cpu1_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average cpu_cpu1 score for the last 1 minutes
        

# cpu_cpu1_softirqs
template: ml_1min_cpu_cpu1_softirqs
      on: cpu.cpu1_softirqs_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average cpu_cpu1_softirqs score for the last 1 minutes
        

# cpu_cpu1_softnet_stat
template: ml_1min_cpu_cpu1_softnet_stat
      on: cpu.cpu1_softnet_stat_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average cpu_cpu1_softnet_stat score for the last 1 minutes
        

# disk_sda
template: ml_1min_disk_sda
      on: disk.sda_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_sda score for the last 1 minutes
        

# disk_avgsz_sda
template: ml_1min_disk_avgsz_sda
      on: disk_avgsz.sda_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_avgsz_sda score for the last 1 minutes
        

# disk_await_sda
template: ml_1min_disk_await_sda
      on: disk_await.sda_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_await_sda score for the last 1 minutes
        

# disk_backlog_sda
template: ml_1min_disk_backlog_sda
      on: disk_backlog.sda_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_backlog_sda score for the last 1 minutes
        

# disk_busy_sda
template: ml_1min_disk_busy_sda
      on: disk_busy.sda_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_busy_sda score for the last 1 minutes
        

# disk_inodes__
template: ml_1min_disk_inodes__
      on: disk_inodes.__km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_inodes__ score for the last 1 minutes
        

# disk_inodes__dev
template: ml_1min_disk_inodes__dev
      on: disk_inodes._dev_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_inodes__dev score for the last 1 minutes
        

# disk_inodes__dev_shm
template: ml_1min_disk_inodes__dev_shm
      on: disk_inodes._dev_shm_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_inodes__dev_shm score for the last 1 minutes
        

# disk_inodes__run
template: ml_1min_disk_inodes__run
      on: disk_inodes._run_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_inodes__run score for the last 1 minutes
        

# disk_inodes__run_lock
template: ml_1min_disk_inodes__run_lock
      on: disk_inodes._run_lock_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_inodes__run_lock score for the last 1 minutes
        

# disk_inodes__run_netdata
template: ml_1min_disk_inodes__run_netdata
      on: disk_inodes._run_netdata_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_inodes__run_netdata score for the last 1 minutes
        

# disk_iotime_sda
template: ml_1min_disk_iotime_sda
      on: disk_iotime.sda_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_iotime_sda score for the last 1 minutes
        

# disk_mops_sda
template: ml_1min_disk_mops_sda
      on: disk_mops.sda_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_mops_sda score for the last 1 minutes
        

# disk_ops_sda
template: ml_1min_disk_ops_sda
      on: disk_ops.sda_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_ops_sda score for the last 1 minutes
        

# disk_space__
template: ml_1min_disk_space__
      on: disk_space.__km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_space__ score for the last 1 minutes
        

# disk_space__boot_efi
template: ml_1min_disk_space__boot_efi
      on: disk_space._boot_efi_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_space__boot_efi score for the last 1 minutes
        

# disk_space__dev
template: ml_1min_disk_space__dev
      on: disk_space._dev_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_space__dev score for the last 1 minutes
        

# disk_space__dev_shm
template: ml_1min_disk_space__dev_shm
      on: disk_space._dev_shm_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_space__dev_shm score for the last 1 minutes
        

# disk_space__run
template: ml_1min_disk_space__run
      on: disk_space._run_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_space__run score for the last 1 minutes
        

# disk_space__run_lock
template: ml_1min_disk_space__run_lock
      on: disk_space._run_lock_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_space__run_lock score for the last 1 minutes
        

# disk_space__run_netdata
template: ml_1min_disk_space__run_netdata
      on: disk_space._run_netdata_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_space__run_netdata score for the last 1 minutes
        

# disk_svctm_sda
template: ml_1min_disk_svctm_sda
      on: disk_svctm.sda_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_svctm_sda score for the last 1 minutes
        

# disk_util_sda
template: ml_1min_disk_util_sda
      on: disk_util.sda_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average disk_util_sda score for the last 1 minutes
        

# ebpf_deleted_objects
template: ml_1min_ebpf_deleted_objects
      on: ebpf.deleted_objects_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ebpf_deleted_objects score for the last 1 minutes
        

# ebpf_exit
template: ml_1min_ebpf_exit
      on: ebpf.exit_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ebpf_exit score for the last 1 minutes
        

# ebpf_file_descriptor
template: ml_1min_ebpf_file_descriptor
      on: ebpf.file_descriptor_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ebpf_file_descriptor score for the last 1 minutes
        

# ebpf_io_bytes
template: ml_1min_ebpf_io_bytes
      on: ebpf.io_bytes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ebpf_io_bytes score for the last 1 minutes
        

# ebpf_io
template: ml_1min_ebpf_io
      on: ebpf.io_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ebpf_io score for the last 1 minutes
        

# ebpf_process_status
template: ml_1min_ebpf_process_status
      on: ebpf.process_status_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ebpf_process_status score for the last 1 minutes
        

# ebpf_process_thread
template: ml_1min_ebpf_process_thread
      on: ebpf.process_thread_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ebpf_process_thread score for the last 1 minutes
        

# ebpf_tcp_functions
template: ml_1min_ebpf_tcp_functions
      on: ebpf.tcp_functions_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ebpf_tcp_functions score for the last 1 minutes
        

# ebpf_tcp_retransmit
template: ml_1min_ebpf_tcp_retransmit
      on: ebpf.tcp_retransmit_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ebpf_tcp_retransmit score for the last 1 minutes
        

# ebpf_total_tcp_bandwidth
template: ml_1min_ebpf_total_tcp_bandwidth
      on: ebpf.total_tcp_bandwidth_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ebpf_total_tcp_bandwidth score for the last 1 minutes
        

# ebpf_total_udp_bandwidth
template: ml_1min_ebpf_total_udp_bandwidth
      on: ebpf.total_udp_bandwidth_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ebpf_total_udp_bandwidth score for the last 1 minutes
        

# ebpf_udp_functions
template: ml_1min_ebpf_udp_functions
      on: ebpf.udp_functions_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ebpf_udp_functions score for the last 1 minutes
        

# groups_cpu
template: ml_1min_groups_cpu
      on: groups.cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_cpu score for the last 1 minutes
        

# groups_cpu_system
template: ml_1min_groups_cpu_system
      on: groups.cpu_system_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_cpu_system score for the last 1 minutes
        

# groups_cpu_user
template: ml_1min_groups_cpu_user
      on: groups.cpu_user_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_cpu_user score for the last 1 minutes
        

# groups_files
template: ml_1min_groups_files
      on: groups.files_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_files score for the last 1 minutes
        

# groups_lreads
template: ml_1min_groups_lreads
      on: groups.lreads_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_lreads score for the last 1 minutes
        

# groups_lwrites
template: ml_1min_groups_lwrites
      on: groups.lwrites_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_lwrites score for the last 1 minutes
        

# groups_major_faults
template: ml_1min_groups_major_faults
      on: groups.major_faults_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_major_faults score for the last 1 minutes
        

# groups_mem
template: ml_1min_groups_mem
      on: groups.mem_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_mem score for the last 1 minutes
        

# groups_minor_faults
template: ml_1min_groups_minor_faults
      on: groups.minor_faults_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_minor_faults score for the last 1 minutes
        

# groups_pipes
template: ml_1min_groups_pipes
      on: groups.pipes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_pipes score for the last 1 minutes
        

# groups_preads
template: ml_1min_groups_preads
      on: groups.preads_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_preads score for the last 1 minutes
        

# groups_processes
template: ml_1min_groups_processes
      on: groups.processes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_processes score for the last 1 minutes
        

# groups_pwrites
template: ml_1min_groups_pwrites
      on: groups.pwrites_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_pwrites score for the last 1 minutes
        

# groups_sockets
template: ml_1min_groups_sockets
      on: groups.sockets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_sockets score for the last 1 minutes
        

# groups_swap
template: ml_1min_groups_swap
      on: groups.swap_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_swap score for the last 1 minutes
        

# groups_threads
template: ml_1min_groups_threads
      on: groups.threads_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_threads score for the last 1 minutes
        

# groups_uptime_avg
template: ml_1min_groups_uptime_avg
      on: groups.uptime_avg_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_uptime_avg score for the last 1 minutes
        

# groups_uptime
template: ml_1min_groups_uptime
      on: groups.uptime_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_uptime score for the last 1 minutes
        

# groups_uptime_max
template: ml_1min_groups_uptime_max
      on: groups.uptime_max_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_uptime_max score for the last 1 minutes
        

# groups_uptime_min
template: ml_1min_groups_uptime_min
      on: groups.uptime_min_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_uptime_min score for the last 1 minutes
        

# groups_vmem
template: ml_1min_groups_vmem
      on: groups.vmem_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average groups_vmem score for the last 1 minutes
        

# ip_ecnpkts
template: ml_1min_ip_ecnpkts
      on: ip.ecnpkts_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ip_ecnpkts score for the last 1 minutes
        

# ip_tcpconnaborts
template: ml_1min_ip_tcpconnaborts
      on: ip.tcpconnaborts_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ip_tcpconnaborts score for the last 1 minutes
        

# ip_tcpofo
template: ml_1min_ip_tcpofo
      on: ip.tcpofo_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ip_tcpofo score for the last 1 minutes
        

# ip_tcpreorders
template: ml_1min_ip_tcpreorders
      on: ip.tcpreorders_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ip_tcpreorders score for the last 1 minutes
        

# ipv4_errors
template: ml_1min_ipv4_errors
      on: ipv4.errors_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_errors score for the last 1 minutes
        

# ipv4_icmp_errors
template: ml_1min_ipv4_icmp_errors
      on: ipv4.icmp_errors_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_icmp_errors score for the last 1 minutes
        

# ipv4_icmp
template: ml_1min_ipv4_icmp
      on: ipv4.icmp_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_icmp score for the last 1 minutes
        

# ipv4_icmpmsg
template: ml_1min_ipv4_icmpmsg
      on: ipv4.icmpmsg_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_icmpmsg score for the last 1 minutes
        

# ipv4_packets
template: ml_1min_ipv4_packets
      on: ipv4.packets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_packets score for the last 1 minutes
        

# ipv4_sockstat_sockets
template: ml_1min_ipv4_sockstat_sockets
      on: ipv4.sockstat_sockets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_sockstat_sockets score for the last 1 minutes
        

# ipv4_sockstat_tcp_mem
template: ml_1min_ipv4_sockstat_tcp_mem
      on: ipv4.sockstat_tcp_mem_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_sockstat_tcp_mem score for the last 1 minutes
        

# ipv4_sockstat_tcp_sockets
template: ml_1min_ipv4_sockstat_tcp_sockets
      on: ipv4.sockstat_tcp_sockets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_sockstat_tcp_sockets score for the last 1 minutes
        

# ipv4_sockstat_udp_mem
template: ml_1min_ipv4_sockstat_udp_mem
      on: ipv4.sockstat_udp_mem_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_sockstat_udp_mem score for the last 1 minutes
        

# ipv4_sockstat_udp_sockets
template: ml_1min_ipv4_sockstat_udp_sockets
      on: ipv4.sockstat_udp_sockets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_sockstat_udp_sockets score for the last 1 minutes
        

# ipv4_tcperrors
template: ml_1min_ipv4_tcperrors
      on: ipv4.tcperrors_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_tcperrors score for the last 1 minutes
        

# ipv4_tcphandshake
template: ml_1min_ipv4_tcphandshake
      on: ipv4.tcphandshake_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_tcphandshake score for the last 1 minutes
        

# ipv4_tcpopens
template: ml_1min_ipv4_tcpopens
      on: ipv4.tcpopens_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_tcpopens score for the last 1 minutes
        

# ipv4_tcppackets
template: ml_1min_ipv4_tcppackets
      on: ipv4.tcppackets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_tcppackets score for the last 1 minutes
        

# ipv4_tcpsock
template: ml_1min_ipv4_tcpsock
      on: ipv4.tcpsock_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_tcpsock score for the last 1 minutes
        

# ipv4_udperrors
template: ml_1min_ipv4_udperrors
      on: ipv4.udperrors_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_udperrors score for the last 1 minutes
        

# ipv4_udppackets
template: ml_1min_ipv4_udppackets
      on: ipv4.udppackets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv4_udppackets score for the last 1 minutes
        

# ipv6_ect
template: ml_1min_ipv6_ect
      on: ipv6.ect_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_ect score for the last 1 minutes
        

# ipv6_icmp
template: ml_1min_ipv6_icmp
      on: ipv6.icmp_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_icmp score for the last 1 minutes
        

# ipv6_icmperrors
template: ml_1min_ipv6_icmperrors
      on: ipv6.icmperrors_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_icmperrors score for the last 1 minutes
        

# ipv6_icmpmldv2
template: ml_1min_ipv6_icmpmldv2
      on: ipv6.icmpmldv2_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_icmpmldv2 score for the last 1 minutes
        

# ipv6_icmpneighbor
template: ml_1min_ipv6_icmpneighbor
      on: ipv6.icmpneighbor_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_icmpneighbor score for the last 1 minutes
        

# ipv6_icmprouter
template: ml_1min_ipv6_icmprouter
      on: ipv6.icmprouter_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_icmprouter score for the last 1 minutes
        

# ipv6_icmptypes
template: ml_1min_ipv6_icmptypes
      on: ipv6.icmptypes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_icmptypes score for the last 1 minutes
        

# ipv6_mcast
template: ml_1min_ipv6_mcast
      on: ipv6.mcast_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_mcast score for the last 1 minutes
        

# ipv6_mcastpkts
template: ml_1min_ipv6_mcastpkts
      on: ipv6.mcastpkts_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_mcastpkts score for the last 1 minutes
        

# ipv6_packets
template: ml_1min_ipv6_packets
      on: ipv6.packets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_packets score for the last 1 minutes
        

# ipv6_sockstat6_raw_sockets
template: ml_1min_ipv6_sockstat6_raw_sockets
      on: ipv6.sockstat6_raw_sockets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_sockstat6_raw_sockets score for the last 1 minutes
        

# ipv6_sockstat6_tcp_sockets
template: ml_1min_ipv6_sockstat6_tcp_sockets
      on: ipv6.sockstat6_tcp_sockets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_sockstat6_tcp_sockets score for the last 1 minutes
        

# ipv6_sockstat6_udp_sockets
template: ml_1min_ipv6_sockstat6_udp_sockets
      on: ipv6.sockstat6_udp_sockets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_sockstat6_udp_sockets score for the last 1 minutes
        

# ipv6_udperrors
template: ml_1min_ipv6_udperrors
      on: ipv6.udperrors_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_udperrors score for the last 1 minutes
        

# ipv6_udppackets
template: ml_1min_ipv6_udppackets
      on: ipv6.udppackets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ipv6_udppackets score for the last 1 minutes
        

# mem_available
template: ml_1min_mem_available
      on: mem.available_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average mem_available score for the last 1 minutes
        

# mem_committed
template: ml_1min_mem_committed
      on: mem.committed_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average mem_committed score for the last 1 minutes
        

# mem_file_segment
template: ml_1min_mem_file_segment
      on: mem.file_segment_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average mem_file_segment score for the last 1 minutes
        

# mem_file_sync
template: ml_1min_mem_file_sync
      on: mem.file_sync_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average mem_file_sync score for the last 1 minutes
        

# mem_kernel
template: ml_1min_mem_kernel
      on: mem.kernel_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average mem_kernel score for the last 1 minutes
        

# mem_memory_map
template: ml_1min_mem_memory_map
      on: mem.memory_map_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average mem_memory_map score for the last 1 minutes
        

# mem_pgfaults
template: ml_1min_mem_pgfaults
      on: mem.pgfaults_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average mem_pgfaults score for the last 1 minutes
        

# mem_slab
template: ml_1min_mem_slab
      on: mem.slab_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average mem_slab score for the last 1 minutes
        

# mem_sync
template: ml_1min_mem_sync
      on: mem.sync_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average mem_sync score for the last 1 minutes
        

# mem_writeback
template: ml_1min_mem_writeback
      on: mem.writeback_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average mem_writeback score for the last 1 minutes
        

# ml_prediction_time
template: ml_1min_ml_prediction_time
      on: ml.prediction_time_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ml_prediction_time score for the last 1 minutes
        

# ml_units
template: ml_1min_ml_units
      on: ml.units_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average ml_units score for the last 1 minutes
        

# net_ens4
template: ml_1min_net_ens4
      on: net.ens4_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average net_ens4 score for the last 1 minutes
        

# net_carrier_ens4
template: ml_1min_net_carrier_ens4
      on: net_carrier.ens4_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average net_carrier_ens4 score for the last 1 minutes
        

# net_duplex_ens4
template: ml_1min_net_duplex_ens4
      on: net_duplex.ens4_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average net_duplex_ens4 score for the last 1 minutes
        

# net_mtu_ens4
template: ml_1min_net_mtu_ens4
      on: net_mtu.ens4_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average net_mtu_ens4 score for the last 1 minutes
        

# net_operstate_ens4
template: ml_1min_net_operstate_ens4
      on: net_operstate.ens4_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average net_operstate_ens4 score for the last 1 minutes
        

# net_packets_ens4
template: ml_1min_net_packets_ens4
      on: net_packets.ens4_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average net_packets_ens4 score for the last 1 minutes
        

# net_speed_ens4
template: ml_1min_net_speed_ens4
      on: net_speed.ens4_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average net_speed_ens4 score for the last 1 minutes
        

# netdata_aclk_cloud_q_recvd_to_processed
template: ml_1min_netdata_aclk_cloud_q_recvd_to_processed
      on: netdata.aclk_cloud_q_recvd_to_processed_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_aclk_cloud_q_recvd_to_processed score for the last 1 minutes
        

# netdata_aclk_cloud_req_cmd
template: ml_1min_netdata_aclk_cloud_req_cmd
      on: netdata.aclk_cloud_req_cmd_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_aclk_cloud_req_cmd score for the last 1 minutes
        

# netdata_aclk_cloud_req
template: ml_1min_netdata_aclk_cloud_req
      on: netdata.aclk_cloud_req_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_aclk_cloud_req score for the last 1 minutes
        

# netdata_aclk_cloud_req_version
template: ml_1min_netdata_aclk_cloud_req_version
      on: netdata.aclk_cloud_req_version_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_aclk_cloud_req_version score for the last 1 minutes
        

# netdata_aclk_db_query_time
template: ml_1min_netdata_aclk_db_query_time
      on: netdata.aclk_db_query_time_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_aclk_db_query_time score for the last 1 minutes
        

# netdata_aclk_query_per_second
template: ml_1min_netdata_aclk_query_per_second
      on: netdata.aclk_query_per_second_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_aclk_query_per_second score for the last 1 minutes
        

# netdata_aclk_query_threads
template: ml_1min_netdata_aclk_query_threads
      on: netdata.aclk_query_threads_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_aclk_query_threads score for the last 1 minutes
        

# netdata_aclk_read_q
template: ml_1min_netdata_aclk_read_q
      on: netdata.aclk_read_q_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_aclk_read_q score for the last 1 minutes
        

# netdata_aclk_status
template: ml_1min_netdata_aclk_status
      on: netdata.aclk_status_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_aclk_status score for the last 1 minutes
        

# netdata_aclk_thread0_cpu
template: ml_1min_netdata_aclk_thread0_cpu
      on: netdata.aclk_thread0_cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_aclk_thread0_cpu score for the last 1 minutes
        

# netdata_aclk_thread1_cpu
template: ml_1min_netdata_aclk_thread1_cpu
      on: netdata.aclk_thread1_cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_aclk_thread1_cpu score for the last 1 minutes
        

# netdata_aclk_write_q
template: ml_1min_netdata_aclk_write_q
      on: netdata.aclk_write_q_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_aclk_write_q score for the last 1 minutes
        

# netdata_apps_children_fix
template: ml_1min_netdata_apps_children_fix
      on: netdata.apps_children_fix_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_apps_children_fix score for the last 1 minutes
        

# netdata_apps_cpu
template: ml_1min_netdata_apps_cpu
      on: netdata.apps_cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_apps_cpu score for the last 1 minutes
        

# netdata_apps_fix
template: ml_1min_netdata_apps_fix
      on: netdata.apps_fix_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_apps_fix score for the last 1 minutes
        

# netdata_apps_sizes
template: ml_1min_netdata_apps_sizes
      on: netdata.apps_sizes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_apps_sizes score for the last 1 minutes
        

# netdata_clients
template: ml_1min_netdata_clients
      on: netdata.clients_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_clients score for the last 1 minutes
        

# netdata_compression_ratio
template: ml_1min_netdata_compression_ratio
      on: netdata.compression_ratio_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_compression_ratio score for the last 1 minutes
        

# netdata_db_points
template: ml_1min_netdata_db_points
      on: netdata.db_points_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_db_points score for the last 1 minutes
        

# netdata_dbengine_compression_ratio
template: ml_1min_netdata_dbengine_compression_ratio
      on: netdata.dbengine_compression_ratio_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_dbengine_compression_ratio score for the last 1 minutes
        

# netdata_dbengine_global_errors
template: ml_1min_netdata_dbengine_global_errors
      on: netdata.dbengine_global_errors_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_dbengine_global_errors score for the last 1 minutes
        

# netdata_dbengine_global_file_descriptors
template: ml_1min_netdata_dbengine_global_file_descriptors
      on: netdata.dbengine_global_file_descriptors_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_dbengine_global_file_descriptors score for the last 1 minutes
        

# netdata_dbengine_io_operations
template: ml_1min_netdata_dbengine_io_operations
      on: netdata.dbengine_io_operations_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_dbengine_io_operations score for the last 1 minutes
        

# netdata_dbengine_io_throughput
template: ml_1min_netdata_dbengine_io_throughput
      on: netdata.dbengine_io_throughput_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_dbengine_io_throughput score for the last 1 minutes
        

# netdata_dbengine_long_term_page_stats
template: ml_1min_netdata_dbengine_long_term_page_stats
      on: netdata.dbengine_long_term_page_stats_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_dbengine_long_term_page_stats score for the last 1 minutes
        

# netdata_dbengine_ram
template: ml_1min_netdata_dbengine_ram
      on: netdata.dbengine_ram_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_dbengine_ram score for the last 1 minutes
        

# netdata_net
template: ml_1min_netdata_net
      on: netdata.net_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_net score for the last 1 minutes
        

# netdata_page_cache_hit_ratio
template: ml_1min_netdata_page_cache_hit_ratio
      on: netdata.page_cache_hit_ratio_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_page_cache_hit_ratio score for the last 1 minutes
        

# netdata_page_cache_stats
template: ml_1min_netdata_page_cache_stats
      on: netdata.page_cache_stats_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_page_cache_stats score for the last 1 minutes
        

# netdata_plugin_cgroups_cpu
template: ml_1min_netdata_plugin_cgroups_cpu
      on: netdata.plugin_cgroups_cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_plugin_cgroups_cpu score for the last 1 minutes
        

# netdata_plugin_diskspace_dt
template: ml_1min_netdata_plugin_diskspace_dt
      on: netdata.plugin_diskspace_dt_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_plugin_diskspace_dt score for the last 1 minutes
        

# netdata_plugin_diskspace
template: ml_1min_netdata_plugin_diskspace
      on: netdata.plugin_diskspace_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_plugin_diskspace score for the last 1 minutes
        

# netdata_plugin_proc_cpu
template: ml_1min_netdata_plugin_proc_cpu
      on: netdata.plugin_proc_cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_plugin_proc_cpu score for the last 1 minutes
        

# netdata_plugin_proc_modules
template: ml_1min_netdata_plugin_proc_modules
      on: netdata.plugin_proc_modules_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_plugin_proc_modules score for the last 1 minutes
        

# netdata_plugin_statsd_charting_cpu
template: ml_1min_netdata_plugin_statsd_charting_cpu
      on: netdata.plugin_statsd_charting_cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_plugin_statsd_charting_cpu score for the last 1 minutes
        

# netdata_plugin_statsd_collector1_cpu
template: ml_1min_netdata_plugin_statsd_collector1_cpu
      on: netdata.plugin_statsd_collector1_cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_plugin_statsd_collector1_cpu score for the last 1 minutes
        

# netdata_plugin_tc_cpu
template: ml_1min_netdata_plugin_tc_cpu
      on: netdata.plugin_tc_cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_plugin_tc_cpu score for the last 1 minutes
        

# netdata_plugin_tc_time
template: ml_1min_netdata_plugin_tc_time
      on: netdata.plugin_tc_time_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_plugin_tc_time score for the last 1 minutes
        

# netdata_private_charts
template: ml_1min_netdata_private_charts
      on: netdata.private_charts_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_private_charts score for the last 1 minutes
        

# netdata_queries
template: ml_1min_netdata_queries
      on: netdata.queries_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_queries score for the last 1 minutes
        

# netdata_requests
template: ml_1min_netdata_requests
      on: netdata.requests_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_requests score for the last 1 minutes
        

# netdata_response_time
template: ml_1min_netdata_response_time
      on: netdata.response_time_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_response_time score for the last 1 minutes
        

# netdata_runtime_anomaliespoc_devml
template: ml_1min_netdata_runtime_anomaliespoc_devml
      on: netdata.runtime_anomaliespoc_devml_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_runtime_anomaliespoc_devml score for the last 1 minutes
        

# netdata_server_cpu
template: ml_1min_netdata_server_cpu
      on: netdata.server_cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_server_cpu score for the last 1 minutes
        

# netdata_statsd_bytes
template: ml_1min_netdata_statsd_bytes
      on: netdata.statsd_bytes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_statsd_bytes score for the last 1 minutes
        

# netdata_statsd_events
template: ml_1min_netdata_statsd_events
      on: netdata.statsd_events_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_statsd_events score for the last 1 minutes
        

# netdata_statsd_metrics
template: ml_1min_netdata_statsd_metrics
      on: netdata.statsd_metrics_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_statsd_metrics score for the last 1 minutes
        

# netdata_statsd_packets
template: ml_1min_netdata_statsd_packets
      on: netdata.statsd_packets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_statsd_packets score for the last 1 minutes
        

# netdata_statsd_reads
template: ml_1min_netdata_statsd_reads
      on: netdata.statsd_reads_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_statsd_reads score for the last 1 minutes
        

# netdata_statsd_useful_metrics
template: ml_1min_netdata_statsd_useful_metrics
      on: netdata.statsd_useful_metrics_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_statsd_useful_metrics score for the last 1 minutes
        

# netdata_tcp_connected
template: ml_1min_netdata_tcp_connected
      on: netdata.tcp_connected_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_tcp_connected score for the last 1 minutes
        

# netdata_tcp_connects
template: ml_1min_netdata_tcp_connects
      on: netdata.tcp_connects_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_tcp_connects score for the last 1 minutes
        

# netdata_web_thread1_cpu
template: ml_1min_netdata_web_thread1_cpu
      on: netdata.web_thread1_cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_web_thread1_cpu score for the last 1 minutes
        

# netdata_web_thread2_cpu
template: ml_1min_netdata_web_thread2_cpu
      on: netdata.web_thread2_cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netdata_web_thread2_cpu score for the last 1 minutes
        

# netfilter_conntrack_changes
template: ml_1min_netfilter_conntrack_changes
      on: netfilter.conntrack_changes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netfilter_conntrack_changes score for the last 1 minutes
        

# netfilter_conntrack_errors
template: ml_1min_netfilter_conntrack_errors
      on: netfilter.conntrack_errors_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netfilter_conntrack_errors score for the last 1 minutes
        

# netfilter_conntrack_expect
template: ml_1min_netfilter_conntrack_expect
      on: netfilter.conntrack_expect_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netfilter_conntrack_expect score for the last 1 minutes
        

# netfilter_conntrack_new
template: ml_1min_netfilter_conntrack_new
      on: netfilter.conntrack_new_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netfilter_conntrack_new score for the last 1 minutes
        

# netfilter_conntrack_search
template: ml_1min_netfilter_conntrack_search
      on: netfilter.conntrack_search_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netfilter_conntrack_search score for the last 1 minutes
        

# netfilter_conntrack_sockets
template: ml_1min_netfilter_conntrack_sockets
      on: netfilter.conntrack_sockets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average netfilter_conntrack_sockets score for the last 1 minutes
        

# services_cpu
template: ml_1min_services_cpu
      on: services.cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average services_cpu score for the last 1 minutes
        

# services_mem_usage
template: ml_1min_services_mem_usage
      on: services.mem_usage_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average services_mem_usage score for the last 1 minutes
        

# services_throttle_io_ops_read
template: ml_1min_services_throttle_io_ops_read
      on: services.throttle_io_ops_read_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average services_throttle_io_ops_read score for the last 1 minutes
        

# services_throttle_io_ops_write
template: ml_1min_services_throttle_io_ops_write
      on: services.throttle_io_ops_write_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average services_throttle_io_ops_write score for the last 1 minutes
        

# services_throttle_io_read
template: ml_1min_services_throttle_io_read
      on: services.throttle_io_read_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average services_throttle_io_read score for the last 1 minutes
        

# services_throttle_io_write
template: ml_1min_services_throttle_io_write
      on: services.throttle_io_write_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average services_throttle_io_write score for the last 1 minutes
        

# system_active_processes
template: ml_1min_system_active_processes
      on: system.active_processes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_active_processes score for the last 1 minutes
        

# system_cpu
template: ml_1min_system_cpu
      on: system.cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_cpu score for the last 1 minutes
        

# system_ctxt
template: ml_1min_system_ctxt
      on: system.ctxt_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_ctxt score for the last 1 minutes
        

# system_entropy
template: ml_1min_system_entropy
      on: system.entropy_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_entropy score for the last 1 minutes
        

# system_forks
template: ml_1min_system_forks
      on: system.forks_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_forks score for the last 1 minutes
        

# system_idlejitter
template: ml_1min_system_idlejitter
      on: system.idlejitter_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_idlejitter score for the last 1 minutes
        

# system_interrupts
template: ml_1min_system_interrupts
      on: system.interrupts_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_interrupts score for the last 1 minutes
        

# system_intr
template: ml_1min_system_intr
      on: system.intr_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_intr score for the last 1 minutes
        

# system_io
template: ml_1min_system_io
      on: system.io_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_io score for the last 1 minutes
        

# system_ip
template: ml_1min_system_ip
      on: system.ip_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_ip score for the last 1 minutes
        

# system_ipc_semaphore_arrays
template: ml_1min_system_ipc_semaphore_arrays
      on: system.ipc_semaphore_arrays_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_ipc_semaphore_arrays score for the last 1 minutes
        

# system_ipc_semaphores
template: ml_1min_system_ipc_semaphores
      on: system.ipc_semaphores_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_ipc_semaphores score for the last 1 minutes
        

# system_ipv6
template: ml_1min_system_ipv6
      on: system.ipv6_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_ipv6 score for the last 1 minutes
        

# system_net
template: ml_1min_system_net
      on: system.net_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_net score for the last 1 minutes
        

# system_pgpgio
template: ml_1min_system_pgpgio
      on: system.pgpgio_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_pgpgio score for the last 1 minutes
        

# system_processes
template: ml_1min_system_processes
      on: system.processes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_processes score for the last 1 minutes
        

# system_ram
template: ml_1min_system_ram
      on: system.ram_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_ram score for the last 1 minutes
        

# system_shared_memory_bytes
template: ml_1min_system_shared_memory_bytes
      on: system.shared_memory_bytes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_shared_memory_bytes score for the last 1 minutes
        

# system_shared_memory_segments
template: ml_1min_system_shared_memory_segments
      on: system.shared_memory_segments_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_shared_memory_segments score for the last 1 minutes
        

# system_softirqs
template: ml_1min_system_softirqs
      on: system.softirqs_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_softirqs score for the last 1 minutes
        

# system_softnet_stat
template: ml_1min_system_softnet_stat
      on: system.softnet_stat_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_softnet_stat score for the last 1 minutes
        

# system_uptime
template: ml_1min_system_uptime
      on: system.uptime_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average system_uptime score for the last 1 minutes
        

# users_cpu
template: ml_1min_users_cpu
      on: users.cpu_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_cpu score for the last 1 minutes
        

# users_cpu_system
template: ml_1min_users_cpu_system
      on: users.cpu_system_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_cpu_system score for the last 1 minutes
        

# users_cpu_user
template: ml_1min_users_cpu_user
      on: users.cpu_user_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_cpu_user score for the last 1 minutes
        

# users_files
template: ml_1min_users_files
      on: users.files_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_files score for the last 1 minutes
        

# users_lreads
template: ml_1min_users_lreads
      on: users.lreads_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_lreads score for the last 1 minutes
        

# users_lwrites
template: ml_1min_users_lwrites
      on: users.lwrites_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_lwrites score for the last 1 minutes
        

# users_major_faults
template: ml_1min_users_major_faults
      on: users.major_faults_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_major_faults score for the last 1 minutes
        

# users_mem
template: ml_1min_users_mem
      on: users.mem_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_mem score for the last 1 minutes
        

# users_minor_faults
template: ml_1min_users_minor_faults
      on: users.minor_faults_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_minor_faults score for the last 1 minutes
        

# users_pipes
template: ml_1min_users_pipes
      on: users.pipes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_pipes score for the last 1 minutes
        

# users_preads
template: ml_1min_users_preads
      on: users.preads_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_preads score for the last 1 minutes
        

# users_processes
template: ml_1min_users_processes
      on: users.processes_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_processes score for the last 1 minutes
        

# users_pwrites
template: ml_1min_users_pwrites
      on: users.pwrites_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_pwrites score for the last 1 minutes
        

# users_sockets
template: ml_1min_users_sockets
      on: users.sockets_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_sockets score for the last 1 minutes
        

# users_swap
template: ml_1min_users_swap
      on: users.swap_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_swap score for the last 1 minutes
        

# users_threads
template: ml_1min_users_threads
      on: users.threads_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_threads score for the last 1 minutes
        

# users_uptime_avg
template: ml_1min_users_uptime_avg
      on: users.uptime_avg_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_uptime_avg score for the last 1 minutes
        

# users_uptime
template: ml_1min_users_uptime
      on: users.uptime_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_uptime score for the last 1 minutes
        

# users_uptime_max
template: ml_1min_users_uptime_max
      on: users.uptime_max_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_uptime_max score for the last 1 minutes
        

# users_uptime_min
template: ml_1min_users_uptime_min
      on: users.uptime_min_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_uptime_min score for the last 1 minutes
        

# users_vmem
template: ml_1min_users_vmem
      on: users.vmem_km
  lookup: average -1m of * foreach *
   units: %
   every: 1m
    warn: $this > (($status >= $WARNING)  ? (75) : (90))
    crit: $this > (($status == $CRITICAL) ? (90) : (150))
    info: average users_vmem score for the last 1 minutes